String[] lines = 

table = loadTable("data.txt", "header"); //adding this secong argument indicates that the first row is the header of the table

join(  );
split(  );
splitTokens(  ); //split by any possible character

IntDict
FloatDict
StringDict //"hash maps" string / int

for()
String s = kingLear[i].toLowerCase();
